# دليل الميزات الجديدة - تطبيق السريع ون

## نظرة عامة
تم إضافة مجموعة شاملة من الميزات المتقدمة لإدارة السائقين والمطاعم والعمليات المالية.

---

## 📊 1. إدارة حسابات السائقين (Driver Management)

### المكونات المضافة:
- **DriverManagementPanel.tsx** - لوحة إدارة السائقين الشاملة
- **DriverWalletManagement.tsx** - إدارة محفظة السائق والسحب

### الميزات:
✅ عرض قائمة جميع السائقين مع فلترة بالاسم/الهاتف
✅ إحصائيات فورية (عدد السائقين المتاحين، الأرباح الإجمالية، متوسط التقييم)
✅ جدول تفصيلي لكل سائق يعرض:
   - الحالة (مفعل/معطل، متاح/مشغول)
   - عدد الطلبات المكملة
   - إجمالي الأرباح
   - متوسط التقييم
   - رصيد المحفظة

✅ نافذة تفاصيل تعرض:
   - معلومات السائق الأساسية
   - إحصائيات الأداء (الطلبات، نسبة النجاح)
   - المعلومات المالية (الأرباح، الرصيد)
   - التقييمات والآراء العملاء

### الاستخدام في الإدارة:
```
إدارة > السائقين > عرض التفاصيل
```

---

## 🏪 2. إدارة حسابات المطاعم (Restaurant Management)

### المكونات المضافة:
- **RestaurantManagementPanel.tsx** - لوحة إدارة المطاعم الشاملة

### الميزات:
✅ عرض قائمة جميع المطاعم مع فلترة بالاسم/الهاتف
✅ إحصائيات مالية فورية:
   - إجمالي المبيعات
   - العمولات المحسوبة
   - المستحقات الإجمالية

✅ جدول تفصيلي لكل مطعم يعرض:
   - الحالة (مفعل/معطل، مفتوح/مغلق)
   - عدد الطلبات
   - إجمالي المبيعات
   - العمولات المحسوبة (10%)
   - الرصيد المستحق
   - متوسط التقييم

✅ نافذة تفاصيل تعرض:
   - معلومات المطعم الكاملة (الهاتف، العنوان)
   - إحصائيات الأداء التفصيلية
   - التفصيل المالي:
     * إجمالي المبيعات
     * العمولات المحسوبة
     * صافي الدخل
     * الرصيد المستحق (مميز بحد أحمر)

### الاستخدام في الإدارة:
```
إدارة > المطاعم > عرض التفاصيل
```

---

## 💰 3. نظام المحفظة والسحب

### جداول قاعدة البيانات:
1. **driver_wallets** - محفظة السائق
   - الرصيد الحالي
   - إجمالي الأرباح
   - إجمالي المسحوب

2. **restaurant_wallets** - محفظة المطعم
   - الرصيد الحالي
   - إجمالي الأرباح
   - إجمالي المسحوب
   - إجمالي العمولات

3. **withdrawal_requests** - طلبات السحب
   - النوع (سائق/مطعم)
   - المبلغ
   - تفاصيل البنك
   - الحالة (معلق/معتمد/مرفوض/محول)

### المميزات:
✅ عرض رصيد المحفظة الفوري
✅ طلب سحب الأموال بسهولة
✅ تتبع حالة طلبات السحب:
   - المعلقة (قيد المراجعة)
   - المعتمدة (جاهزة للتحويل)
   - المرفوضة

✅ سجل كامل لجميع عمليات السحب

### المسارات API:
```
GET  /api/drivers/:driverId/wallet
POST /api/drivers/:driverId/wallet/add-balance
POST /api/withdrawal-requests
GET  /api/admin/withdrawal-requests/pending
POST /api/admin/withdrawal-requests/:requestId/approve
POST /api/admin/withdrawal-requests/:requestId/reject
```

---

## 📈 4. التقارير المتقدمة

### الصفحة:
- **AdvancedReports.tsx** - صفحة التقارير المتقدمة

### التقارير المتاحة:

#### أ) تقرير السائقين:
✅ إجمالي الأرباح والطلبات
✅ متوسط التقييم الإجمالي
✅ جدول مفصل يعرض:
   - اسم السائق
   - عدد الطلبات
   - الأرباح
   - التقييم
   - الرصيد الحالي
   - حالة التفعيل

#### ب) تقرير المطاعم:
✅ إجمالي المبيعات والعمولات
✅ صافي الدخل الكلي
✅ جدول مفصل يعرض:
   - اسم المطعم
   - عدد الطلبات
   - إجمالي المبيعات
   - العمولات
   - المستحق
   - حالة التفعيل

#### ج) التقارير المالية:
✅ طلبات السحب المعلقة
✅ التفاصيل:
   - نوع الطلب (سائق/مطعم)
   - المبلغ
   - البنك
   - صاحب الحساب
   - التاريخ

### تحميل التقارير:
✅ تنزيل بصيغة CSV
✅ يمكن استخدامها في Excel/Google Sheets

### المسارات API:
```
GET /api/admin/drivers-summary
GET /api/admin/restaurants-summary
GET /api/admin/drivers/:driverId/performance
GET /api/admin/restaurants/:restaurantId/performance
GET /api/admin/withdrawal-requests/pending
```

---

## 🗄️ 5. جداول قاعدة البيانات الجديدة

### تم إضافة الجداول التالية:

1. **driver_reviews** - تقييمات السائقين
   - rating: التقييم (1-5)
   - comment: التعليق
   - deliveryTime: وقت التوصيل

2. **driver_earnings** - إحصائيات أرباح السائق
   - totalEarnings: إجمالي الأرباح
   - todayEarnings: أرباح اليوم
   - weekEarnings: أرباح الأسبوع
   - monthEarnings: أرباح الشهر
   - averageRating: متوسط التقييم

3. **commission_settings** - إعدادات العمولات
   - type: نوع العمولة (افتراضية/مطعم/سائق)
   - commissionPercent: نسبة العمولة
   - minAmount/maxAmount: الحد الأدنى والأقصى

4. **withdrawal_requests** - طلبات السحب
5. **audit_logs** - سجل العمليات
6. **driver_work_sessions** - جلسات عمل السائقين

---

## 🔌 6. المسارات API الجديدة

### إدارة السائقين:
```
GET  /api/admin/drivers/:driverId/details
GET  /api/admin/drivers-summary
GET  /api/admin/drivers/:driverId/reviews
GET  /api/admin/drivers/:driverId/performance
GET  /api/admin/drivers/:driverId/work-sessions
```

### إدارة المطاعم:
```
GET  /api/admin/restaurants/:restaurantId/details
GET  /api/admin/restaurants-summary
GET  /api/admin/restaurants/:restaurantId/performance
```

### المحفظة والسحب:
```
GET  /api/drivers/:driverId/wallet
POST /api/drivers/:driverId/wallet/add-balance
POST /api/withdrawal-requests
GET  /api/admin/withdrawal-requests/pending
POST /api/admin/withdrawal-requests/:requestId/approve
POST /api/admin/withdrawal-requests/:requestId/reject
```

### سجل العمليات:
```
GET  /api/admin/audit-logs
POST /api/audit-logs
```

### العمولات:
```
GET  /api/admin/commission-settings
```

---

## 🔄 7. التكامل بين الأجزاء

### تدفق البيانات:

```
العميل                 السائق              الخادم               قاعدة البيانات
  |                    |                    |                    |
  |---- طلب -------->  |                    |                    |
  |                    |---- قبول -------->  |---- INSERT ---->   |
  |                    |                    |  orders            |
  |                    |---- تحديث ---->     |---- UPDATE ---->   |
  |                    |  موقع              |  orders            |
  |  <-- تحديث --------| <-- سحب ------      |  <---- SELECT ----  |
  |     موقع          |     بيانات          |                    |
  |                    |                    |---- UPDATE ---->   |
  |                    |                    |  driver_wallets    |
  |                    |---- اكمل -------->  |---- UPDATE ---->   |
  |                    |                    |  orders            |
  |                    |                    |  driver_earnings   |
  |  <-- تأكيد --------| <-- إشعار ------    |  driver_wallets    |
  |     الاستلام      |                    |                    |
```

### تدفق السحب:

```
السائق                 الخادم               الإدارة             قاعدة البيانات
  |                    |                    |                    |
  |-- طلب سحب -------> |                    |                    |
  |                    |---- INSERT ------> |                    | ------>
  |                    |  withdrawal_      |                    | INSERT
  |                    |  requests         |                    | withdrawal
  |                    |                    |                    |
  |                    |                    | <-- عرض معلق ----   | SELECT
  |                    |                    |                    |
  |                    |                    |-- موافقة -------> |
  |                    |                    |                    | UPDATE
  |                    |-- إشعار موافقة ---|                    | withdrawal
  |                    |                    |                    | & wallet
  | <-- تأكيد دخول ----| <-- سحب من محفظة  |                    |
  |    الأموال        |                    |                    |
```

---

## 📱 8. واجهات المستخدم

### لوحة التحكم (Admin):
- **قسم السائقين**: عرض، بحث، إحصائيات، تفاصيل مفصلة
- **قسم المطاعم**: عرض، بحث، إحصائيات مالية، تفاصيل مفصلة
- **التقارير**: تقارير شاملة، تحميل CSV
- **المحفظة والسحب**: إدارة طلبات السحب

### تطبيق السائق:
- **صفحة المحفظة**: عرض الرصيد والأرباح
- **طلب السحب**: نموذج بسيط لطلب السحب
- **سجل السحب**: تتبع جميع طلبات السحب السابقة

---

## 🚀 9. خطوات التفعيل

### 1. تحديث قاعدة البيانات:
```bash
npm run db:push
```

### 2. تفعيل الـ Routes في server/routes.ts:
```typescript
import { registerAdvancedRoutes } from "./routes/advanced";

// في دالة registerRoutes:
registerAdvancedRoutes(app);
```

### 3. دمج المكونات في الواجهات:
- إضافة DriverManagementPanel في AdminDashboard
- إضافة RestaurantManagementPanel في AdminDashboard
- إضافة AdvancedReports صفحة جديدة في الإدارة
- إضافة DriverWalletManagement في تطبيق السائق

### 4. الاختبار:
```bash
npm run dev
```

---

## 📋 10. معايير الحساب

### حساب العمولات:
- العمولة الافتراضية: **10%**
- تُحسب على إجمالي سعر الطلب
- مثال: طلب بقيمة 100 ريال = 10 ريال عمولة

### حساب أرباح السائق:
- رسوم التوصيل الفعلية = الربح
- توزيع العمولات حسب إعدادات النظام

### حساب رصيد المطعم:
- رصيد = (إجمالي المبيعات) - (العمولات المحسوبة) - (المسحوب سابقاً)

---

## ⚠️ 11. ملاحظات مهمة

1. **الأمان**: جميع العمليات المالية تتطلب موافقة الإدارة
2. **الدقة**: استخدم decimal(10,2) لجميع العمليات المالية
3. **التتبع**: جميع العمليات تسجل في audit_logs
4. **التحديث**: البيانات تتحدث فوراً عند كل عملية
5. **الملف الشخصي**: كل سائق ومطعم لهم محفظة خاصة بهم

---

## 🔍 12. استكشاف الأخطاء

### المشكلة: الرصيد لا يتحدث
**الحل**: تأكد من تحديث driver_wallets/restaurant_wallets بشكل صحيح

### المشكلة: طلبات السحب لا تظهر
**الحل**: تأكد من استدعاء API `/api/admin/withdrawal-requests/pending`

### المشكلة: الأرباح غير صحيحة
**الحل**: تحقق من حساب العمولات والرسوم بشكل صحيح

---

## 📚 ملفات الكود الرئيسية

```
shared/
├── schema.ts                      # 8 جداول جديدة + 8 schemas

server/
├── db-advanced.ts                # دوال السائقين والمطاعم والمحافظ
├── routes/
│   └── advanced.ts                # API endpoints جديدة

client/src/
├── components/
│   ├── admin/
│   │   ├── DriverManagementPanel.tsx
│   │   └── RestaurantManagementPanel.tsx
│   └── DriverWalletManagement.tsx
└── pages/
    └── admin/
        └── AdvancedReports.tsx
```

---

**آخر تحديث**: 07/01/2026
